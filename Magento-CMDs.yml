mageTV:
	from magento 2.2.5
Vessel cmds: 
./vessel
	n98 	db:import --compression=gzip /db/dbname.sql.gz
	n98 	setup:upgrade
	magento				List all cmds
	cf					cache flush
	cc
					
		start			
		cache:flush
		indexer:reindex
		shell				get into container terminal
		setup:upgrade			Upgrades the Magento application, DB data, and schema
		module:status			lists enabled/disabled modules
		module:enable <name>
		admin:user:create
		info:adminuri
				


